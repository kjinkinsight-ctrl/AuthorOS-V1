/// Desktop and mobile implementation of [configureUrlStrategy].
///
/// There is no address bar to configure, so this does nothing. See
/// `url_strategy.dart`.
library;

void configureUrlStrategy() {}
